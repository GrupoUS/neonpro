{
  "framework": "vite",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "cd packages/types && pnpm run build && cd ../ui && pnpm run build && cd ../config && pnpm run build && cd ../utils && pnpm run build && cd ../../apps/web && pnpm run build",
  "outputDirectory": "apps/web/dist",
  "git": {
    "deploymentEnabled": { "preview": true, "production": true }
  }
}
