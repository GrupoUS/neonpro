{
  "version": 2,
  "buildCommand": "npx prisma generate && next build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "env": {
    "PRISMA_CLI_BINARY_TARGETS": "rhel-openssl-1.0.x,linux-musl",
    "SKIP_PRISMA_GENERATE_ON_VERCEL": "false"
  }
}