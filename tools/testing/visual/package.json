{
  "name": "@neonpro/visual-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Visual regression tests for NeonPro Healthcare Platform",
  "scripts": {
    "test": "echo 'Visual E2E tests disabled (Playwright removed). Use Vitest for unit/integration.'",
    "screenshots:clean": "rimraf test-results-*/ && rimraf playwright-report/"
  },
  "dependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "rimraf": "^5.0.0"
  }
}
