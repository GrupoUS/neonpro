// Healthcare validators placeholder
export const validateCRM = (crm: string): boolean => true;
export const validateCPF = (cpf: string): boolean => true;
export const validateMedicalRecord = (record: string): boolean => true;