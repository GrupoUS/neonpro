// Backend service for Automated Protocol Optimization System
// Story 9.3: Automated protocol optimization with continuous improvement
// Generated by: VoidBeast V4.0
var __assign =
  (this && this.__assign) ||
  function () {
    __assign =
      Object.assign ||
      ((t) => {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
          s = arguments[i];
          for (var p in s) if (Object.hasOwn(s, p)) t[p] = s[p];
        }
        return t;
      });
    return __assign.apply(this, arguments);
  };
var __awaiter =
  (this && this.__awaiter) ||
  ((thisArg, _arguments, P, generator) => {
    function adopt(value) {
      return value instanceof P
        ? value
        : new P((resolve) => {
            resolve(value);
          });
    }
    return new (P || (P = Promise))((resolve, reject) => {
      function fulfilled(value) {
        try {
          step(generator.next(value));
        } catch (e) {
          reject(e);
        }
      }
      function rejected(value) {
        try {
          step(generator["throw"](value));
        } catch (e) {
          reject(e);
        }
      }
      function step(result) {
        result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
      }
      step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
  });
var __generator =
  (this && this.__generator) ||
  ((thisArg, body) => {
    var _ = {
        label: 0,
        sent: () => {
          if (t[0] & 1) throw t[1];
          return t[1];
        },
        trys: [],
        ops: [],
      },
      f,
      y,
      t,
      g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return (
      (g.next = verb(0)),
      (g["throw"] = verb(1)),
      (g["return"] = verb(2)),
      typeof Symbol === "function" &&
        (g[Symbol.iterator] = function () {
          return this;
        }),
      g
    );
    function verb(n) {
      return (v) => step([n, v]);
    }
    function step(op) {
      if (f) throw new TypeError("Generator is already executing.");
      while ((g && ((g = 0), op[0] && (_ = 0)), _))
        try {
          if (
            ((f = 1),
            y &&
              (t =
                op[0] & 2
                  ? y["return"]
                  : op[0]
                    ? y["throw"] || ((t = y["return"]) && t.call(y), 0)
                    : y.next) &&
              !(t = t.call(y, op[1])).done)
          )
            return t;
          if (((y = 0), t)) op = [op[0] & 2, t.value];
          switch (op[0]) {
            case 0:
            case 1:
              t = op;
              break;
            case 4:
              _.label++;
              return { value: op[1], done: false };
            case 5:
              _.label++;
              y = op[1];
              op = [0];
              continue;
            case 7:
              op = _.ops.pop();
              _.trys.pop();
              continue;
            default:
              if (
                !((t = _.trys), (t = t.length > 0 && t[t.length - 1])) &&
                (op[0] === 6 || op[0] === 2)
              ) {
                _ = 0;
                continue;
              }
              if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) {
                _.label = op[1];
                break;
              }
              if (op[0] === 6 && _.label < t[1]) {
                _.label = t[1];
                t = op;
                break;
              }
              if (t && _.label < t[2]) {
                _.label = t[2];
                _.ops.push(op);
                break;
              }
              if (t[2]) _.ops.pop();
              _.trys.pop();
              continue;
          }
          op = body.call(thisArg, _);
        } catch (e) {
          op = [6, e];
          y = 0;
        } finally {
          f = t = 0;
        }
      if (op[0] & 5) throw op[1];
      return { value: op[0] ? op[1] : void 0, done: true };
    }
  });
Object.defineProperty(exports, "__esModule", { value: true });
exports.AutomatedProtocolOptimizationService = void 0;
var client_1 = require("../../utils/supabase/client");
var AutomatedProtocolOptimizationService = /** @class */ (() => {
  function AutomatedProtocolOptimizationService() {}
  // Supabase client created per method for proper request context
  // Protocol Version Management
  AutomatedProtocolOptimizationService.prototype.createProtocolVersion = function (data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error, _b, _c, _d;
      var _e;
      var _f;
      return __generator(this, (_g) => {
        switch (_g.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _g.sent();
            _c = (_b = supabase.from("protocol_versions")).insert;
            _d = [__assign({}, data)];
            _e = {};
            return [4 /*yield*/, supabase.auth.getUser()];
          case 2:
            return [
              4 /*yield*/,
              _c
                .apply(_b, [
                  __assign.apply(
                    void 0,
                    _d.concat([
                      ((_e.created_by =
                        (_f = _g.sent().data.user) === null || _f === void 0 ? void 0 : _f.id),
                      (_e.created_at = new Date().toISOString()),
                      (_e.updated_at = new Date().toISOString()),
                      _e),
                    ]),
                  ),
                ])
                .select()
                .single(),
            ];
          case 3:
            (_a = _g.sent()), (result = _a.data), (error = _a.error);
            if (error) throw new Error("Failed to create protocol version: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolVersions = function (filters) {
    return __awaiter(this, void 0, void 0, function () {
      var query, page, per_page, from, to, _a, data, error, count, protocolsWithCounts;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            query = supabase
              .from("protocol_versions")
              .select("\n        *,\n        latest_analytics:protocol_analytics(*)\n      ");
            if (filters === null || filters === void 0 ? void 0 : filters.protocol_name) {
              query = query.ilike("protocol_name", "%".concat(filters.protocol_name, "%"));
            }
            if (filters === null || filters === void 0 ? void 0 : filters.approval_status) {
              query = query.eq("approval_status", filters.approval_status);
            }
            if (
              (filters === null || filters === void 0 ? void 0 : filters.is_active) !== undefined
            ) {
              query = query.eq("is_active", filters.is_active);
            }
            page = (filters === null || filters === void 0 ? void 0 : filters.page) || 1;
            per_page = (filters === null || filters === void 0 ? void 0 : filters.per_page) || 20;
            from = (page - 1) * per_page;
            to = from + per_page - 1;
            return [4 /*yield*/, query.range(from, to).order("created_at", { ascending: false })];
          case 1:
            (_a = _b.sent()), (data = _a.data), (error = _a.error), (count = _a.count);
            if (error) throw new Error("Failed to fetch protocol versions: ".concat(error.message));
            return [
              4 /*yield*/,
              Promise.all(
                (data || []).map((protocol) =>
                  __awaiter(this, void 0, void 0, function () {
                    var _a, outcomeCount, feedbackCount;
                    return __generator(this, function (_b) {
                      switch (_b.label) {
                        case 0:
                          return [
                            4 /*yield*/,
                            Promise.all([
                              this.getProtocolOutcomeCount(protocol.id),
                              this.getProtocolFeedbackCount(protocol.id),
                            ]),
                          ];
                        case 1:
                          (_a = _b.sent()), (outcomeCount = _a[0]), (feedbackCount = _a[1]);
                          return [
                            2 /*return*/,
                            __assign(__assign({}, protocol), {
                              outcome_count: outcomeCount,
                              feedback_count: feedbackCount,
                            }),
                          ];
                      }
                    });
                  }),
                ),
              ),
            ];
          case 2:
            protocolsWithCounts = _b.sent();
            return [2 /*return*/, { data: protocolsWithCounts, total: count || 0 }];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolVersionById = function (id) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, data, error;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _b.sent();
            return [
              4 /*yield*/,
              supabase.from("protocol_versions").select("*").eq("id", id).single(),
            ];
          case 2:
            (_a = _b.sent()), (data = _a.data), (error = _a.error);
            if (error) throw new Error("Failed to fetch protocol version: ".concat(error.message));
            return [2 /*return*/, data];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.updateProtocolVersion = function (id, data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _b.sent();
            return [
              4 /*yield*/,
              supabase
                .from("protocol_versions")
                .update(__assign(__assign({}, data), { updated_at: new Date().toISOString() }))
                .eq("id", id)
                .select()
                .single(),
            ];
          case 2:
            (_a = _b.sent()), (result = _a.data), (error = _a.error);
            if (error) throw new Error("Failed to update protocol version: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.deleteProtocolVersion = function (id) {
    return __awaiter(this, void 0, void 0, function () {
      var error;
      return __generator(this, (_a) => {
        switch (_a.label) {
          case 0:
            return [4 /*yield*/, supabase.from("protocol_versions").delete().eq("id", id)];
          case 1:
            error = _a.sent().error;
            if (error) throw new Error("Failed to delete protocol version: ".concat(error.message));
            return [2 /*return*/];
        }
      });
    });
  };
  // Protocol Outcome Tracking
  AutomatedProtocolOptimizationService.prototype.createProtocolOutcome = function (data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error, _b, _c, _d;
      var _e;
      var _f;
      return __generator(this, function (_g) {
        switch (_g.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _g.sent();
            _c = (_b = supabase.from("protocol_outcomes")).insert;
            _d = [__assign({}, data)];
            _e = {};
            return [4 /*yield*/, supabase.auth.getUser()];
          case 2:
            return [
              4 /*yield*/,
              _c
                .apply(_b, [
                  __assign.apply(
                    void 0,
                    _d.concat([
                      ((_e.recorded_by =
                        (_f = _g.sent().data.user) === null || _f === void 0 ? void 0 : _f.id),
                      (_e.created_at = new Date().toISOString()),
                      (_e.updated_at = new Date().toISOString()),
                      _e),
                    ]),
                  ),
                ])
                .select()
                .single(),
            ];
          case 3:
            (_a = _g.sent()), (result = _a.data), (error = _a.error);
            if (error) throw new Error("Failed to create protocol outcome: ".concat(error.message));
            // Trigger optimization analysis after recording outcome
            return [4 /*yield*/, this.triggerOptimizationAnalysis(data.protocol_version_id)];
          case 4:
            // Trigger optimization analysis after recording outcome
            _g.sent();
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolOutcomes = function (filters) {
    return __awaiter(this, void 0, void 0, function () {
      var query, page, per_page, from, to, _a, data, error, count;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            query = supabase.from("protocol_outcomes").select("*");
            if (filters === null || filters === void 0 ? void 0 : filters.protocol_version_id) {
              query = query.eq("protocol_version_id", filters.protocol_version_id);
            }
            if (filters === null || filters === void 0 ? void 0 : filters.patient_id) {
              query = query.eq("patient_id", filters.patient_id);
            }
            if (
              (filters === null || filters === void 0 ? void 0 : filters.success_score_min) !==
              undefined
            ) {
              query = query.gte("success_score", filters.success_score_min);
            }
            if (
              (filters === null || filters === void 0 ? void 0 : filters.success_score_max) !==
              undefined
            ) {
              query = query.lte("success_score", filters.success_score_max);
            }
            page = (filters === null || filters === void 0 ? void 0 : filters.page) || 1;
            per_page = (filters === null || filters === void 0 ? void 0 : filters.per_page) || 20;
            from = (page - 1) * per_page;
            to = from + per_page - 1;
            return [4 /*yield*/, query.range(from, to).order("outcome_date", { ascending: false })];
          case 1:
            (_a = _b.sent()), (data = _a.data), (error = _a.error), (count = _a.count);
            if (error) throw new Error("Failed to fetch protocol outcomes: ".concat(error.message));
            return [2 /*return*/, { data: data || [], total: count || 0 }];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolOutcomeCount = function (
    protocol_version_id,
  ) {
    return __awaiter(this, void 0, void 0, function () {
      var _a, count, error;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            return [
              4 /*yield*/,
              supabase
                .from("protocol_outcomes")
                .select("*", { count: "exact", head: true })
                .eq("protocol_version_id", protocol_version_id),
            ];
          case 1:
            (_a = _b.sent()), (count = _a.count), (error = _a.error);
            if (error) throw new Error("Failed to get outcome count: ".concat(error.message));
            return [2 /*return*/, count || 0];
        }
      });
    });
  };
  // Protocol Feedback Management
  AutomatedProtocolOptimizationService.prototype.createProtocolFeedback = function (data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error, _b, _c, _d;
      var _e;
      var _f;
      return __generator(this, function (_g) {
        switch (_g.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _g.sent();
            _c = (_b = supabase.from("protocol_feedback")).insert;
            _d = [__assign({}, data)];
            _e = {};
            return [4 /*yield*/, supabase.auth.getUser()];
          case 2:
            return [
              4 /*yield*/,
              _c
                .apply(_b, [
                  __assign.apply(
                    void 0,
                    _d.concat([
                      ((_e.provider_id =
                        (_f = _g.sent().data.user) === null || _f === void 0 ? void 0 : _f.id),
                      (_e.created_at = new Date().toISOString()),
                      (_e.updated_at = new Date().toISOString()),
                      _e),
                    ]),
                  ),
                ])
                .select()
                .single(),
            ];
          case 3:
            (_a = _g.sent()), (result = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to create protocol feedback: ".concat(error.message));
            if (!(data.priority_level === "high" || data.priority_level === "critical"))
              return [3 /*break*/, 5];
            return [4 /*yield*/, this.processPriorityFeedback(result.id)];
          case 4:
            _g.sent();
            _g.label = 5;
          case 5:
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolFeedback = function (filters) {
    return __awaiter(this, void 0, void 0, function () {
      var query, page, per_page, from, to, _a, data, error, count;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            query = supabase.from("protocol_feedback").select("*");
            if (filters === null || filters === void 0 ? void 0 : filters.protocol_id) {
              query = query.eq("protocol_id", filters.protocol_id);
            }
            if (filters === null || filters === void 0 ? void 0 : filters.provider_id) {
              query = query.eq("provider_id", filters.provider_id);
            }
            if (filters === null || filters === void 0 ? void 0 : filters.feedback_type) {
              query = query.eq("feedback_type", filters.feedback_type);
            }
            if (filters === null || filters === void 0 ? void 0 : filters.status) {
              query = query.eq("status", filters.status);
            }
            if (filters === null || filters === void 0 ? void 0 : filters.priority_level) {
              query = query.eq("priority_level", filters.priority_level);
            }
            page = (filters === null || filters === void 0 ? void 0 : filters.page) || 1;
            per_page = (filters === null || filters === void 0 ? void 0 : filters.per_page) || 20;
            from = (page - 1) * per_page;
            to = from + per_page - 1;
            return [4 /*yield*/, query.range(from, to).order("created_at", { ascending: false })];
          case 1:
            (_a = _b.sent()), (data = _a.data), (error = _a.error), (count = _a.count);
            if (error) throw new Error("Failed to fetch protocol feedback: ".concat(error.message));
            return [2 /*return*/, { data: data || [], total: count || 0 }];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.updateProtocolFeedback = function (id, data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error, _b, _c, _d;
      var _e;
      var _f;
      return __generator(this, (_g) => {
        switch (_g.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _g.sent();
            _c = (_b = supabase.from("protocol_feedback")).update;
            _d = [__assign({}, data)];
            _e = {};
            return [4 /*yield*/, supabase.auth.getUser()];
          case 2:
            return [
              4 /*yield*/,
              _c
                .apply(_b, [
                  __assign.apply(
                    void 0,
                    _d.concat([
                      ((_e.reviewed_by =
                        (_f = _g.sent().data.user) === null || _f === void 0 ? void 0 : _f.id),
                      (_e.reviewed_at = new Date().toISOString()),
                      (_e.updated_at = new Date().toISOString()),
                      _e),
                    ]),
                  ),
                ])
                .eq("id", id)
                .select()
                .single(),
            ];
          case 3:
            (_a = _g.sent()), (result = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to update protocol feedback: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolFeedbackCount = function (protocol_id) {
    return __awaiter(this, void 0, void 0, function () {
      var _a, count, error;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            return [
              4 /*yield*/,
              supabase
                .from("protocol_feedback")
                .select("*", { count: "exact", head: true })
                .eq("protocol_id", protocol_id),
            ];
          case 1:
            (_a = _b.sent()), (count = _a.count), (error = _a.error);
            if (error) throw new Error("Failed to get feedback count: ".concat(error.message));
            return [2 /*return*/, count || 0];
        }
      });
    });
  };
  // Optimization Analysis Engine
  AutomatedProtocolOptimizationService.prototype.triggerOptimizationAnalysis = function (
    protocol_id,
  ) {
    return __awaiter(this, void 0, void 0, function () {
      var _a, protocol, outcomes, feedback, analysis, _b, result, error;
      return __generator(this, function (_c) {
        switch (_c.label) {
          case 0:
            return [
              4 /*yield*/,
              Promise.all([
                this.getProtocolVersionById(protocol_id),
                this.getProtocolOutcomes({ protocol_version_id: protocol_id }),
                this.getProtocolFeedback({ protocol_id: protocol_id }),
              ]),
            ];
          case 1:
            (_a = _c.sent()), (protocol = _a[0]), (outcomes = _a[1]), (feedback = _a[2]);
            if (!protocol) {
              throw new Error("Protocol not found for optimization analysis");
            }
            return [
              4 /*yield*/,
              this.performOptimizationAnalysis(protocol, outcomes.data, feedback.data),
            ];
          case 2:
            analysis = _c.sent();
            return [
              4 /*yield*/,
              supabase
                .from("optimization_results")
                .insert({
                  protocol_id: protocol_id,
                  optimization_type: "automated_analysis",
                  improvement_metrics: analysis.metrics,
                  validation_data: analysis.validation,
                  confidence_score: analysis.confidence,
                  statistical_significance: analysis.significance,
                  recommendation_data: analysis.recommendations,
                  automated_analysis: analysis.full_analysis,
                  human_review_required: analysis.confidence < 80,
                  created_at: new Date().toISOString(),
                  updated_at: new Date().toISOString(),
                })
                .select()
                .single(),
            ];
          case 3:
            (_b = _c.sent()), (result = _b.data), (error = _b.error);
            if (error)
              throw new Error("Failed to store optimization results: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.performOptimizationAnalysis = function (
    protocol,
    outcomes,
    feedback,
  ) {
    return __awaiter(this, void 0, void 0, function () {
      var successRate,
        averageSatisfaction,
        complicationRate,
        criticalFeedback,
        improvementSuggestions,
        recommendations,
        confidence,
        significance;
      return __generator(this, (_a) => {
        successRate =
          outcomes.length > 0
            ? outcomes.reduce((sum, o) => sum + o.success_score, 0) / outcomes.length
            : 0;
        averageSatisfaction =
          outcomes
            .filter((o) => o.patient_satisfaction !== null)
            .reduce((sum, o) => sum + (o.patient_satisfaction || 0), 0) /
            outcomes.filter((o) => o.patient_satisfaction !== null).length || 0;
        complicationRate =
          (outcomes.filter((o) => Array.isArray(o.complications) && o.complications.length > 0)
            .length /
            outcomes.length) *
          100;
        criticalFeedback = feedback.filter((f) => f.priority_level === "critical").length;
        improvementSuggestions = feedback.filter(
          (f) => f.feedback_type === "improvement" || f.feedback_type === "suggestion",
        ).length;
        recommendations = [];
        if (successRate < 75) {
          recommendations.push({
            type: "success_rate_improvement",
            priority: "high",
            suggestion: "Consider reviewing protocol steps with low success rates",
            potential_improvement: 15,
          });
        }
        if (complicationRate > 10) {
          recommendations.push({
            type: "complication_reduction",
            priority: "critical",
            suggestion: "Implement additional safety measures to reduce complications",
            potential_improvement: 20,
          });
        }
        if (averageSatisfaction < 7) {
          recommendations.push({
            type: "satisfaction_improvement",
            priority: "medium",
            suggestion: "Review patient communication and expectations management",
            potential_improvement: 10,
          });
        }
        confidence = Math.min(
          100,
          outcomes.length * 2 + feedback.length * 1.5 + (protocol.optimization_data ? 10 : 0),
        );
        significance = outcomes.length >= 30 ? 0.05 : 0.1;
        return [
          2 /*return*/,
          {
            metrics: {
              success_rate: successRate,
              average_satisfaction: averageSatisfaction,
              complication_rate: complicationRate,
              total_outcomes: outcomes.length,
              total_feedback: feedback.length,
              critical_feedback_count: criticalFeedback,
            },
            validation: {
              sample_size: outcomes.length,
              feedback_quality: feedback.length > 0 ? "adequate" : "limited",
              data_completeness: outcomes.length > 10 ? "good" : "limited",
            },
            confidence: confidence,
            significance: significance,
            recommendations: {
              high_priority: recommendations.filter(
                (r) => r.priority === "critical" || r.priority === "high",
              ),
              medium_priority: recommendations.filter((r) => r.priority === "medium"),
              all_recommendations: recommendations,
            },
            full_analysis: {
              analyzed_at: new Date().toISOString(),
              protocol_version: protocol.version_number,
              analysis_scope: {
                outcomes_period:
                  outcomes.length > 0
                    ? {
                        start: Math.min.apply(
                          Math,
                          outcomes.map((o) => new Date(o.outcome_date).getTime()),
                        ),
                        end: Math.max.apply(
                          Math,
                          outcomes.map((o) => new Date(o.outcome_date).getTime()),
                        ),
                      }
                    : null,
                feedback_summary: {
                  total: feedback.length,
                  by_type: feedback.reduce((acc, f) => {
                    acc[f.feedback_type] = (acc[f.feedback_type] || 0) + 1;
                    return acc;
                  }, {}),
                },
              },
            },
          },
        ];
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getOptimizationResults = function (protocol_id) {
    return __awaiter(this, void 0, void 0, function () {
      var query, _a, data, error, results;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            query = supabase.from("optimization_results").select("*");
            if (protocol_id) {
              query = query.eq("protocol_id", protocol_id);
            }
            return [4 /*yield*/, query.order("created_at", { ascending: false })];
          case 1:
            (_a = _b.sent()), (data = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to fetch optimization results: ".concat(error.message));
            results = data || [];
            return [
              2 /*return*/,
              {
                high_priority: results.filter((r) => {
                  var _a, _b, _c;
                  return (
                    r.confidence_score &&
                    r.confidence_score >= 80 &&
                    ((_c =
                      (_b =
                        (_a = r.improvement_metrics) === null || _a === void 0
                          ? void 0
                          : _a.recommendations) === null || _b === void 0
                        ? void 0
                        : _b.high_priority) === null || _c === void 0
                      ? void 0
                      : _c.length) > 0
                  );
                }),
                medium_priority: results.filter(
                  (r) => r.confidence_score && r.confidence_score >= 60 && r.confidence_score < 80,
                ),
                low_priority: results.filter((r) => r.confidence_score && r.confidence_score < 60),
                review_required: results.filter((r) => r.human_review_required),
              },
            ];
        }
      });
    });
  };
  // A/B Testing and Experiments
  AutomatedProtocolOptimizationService.prototype.createProtocolExperiment = function (data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error, _b, _c, _d;
      var _e;
      var _f;
      return __generator(this, (_g) => {
        switch (_g.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _g.sent();
            _c = (_b = supabase.from("protocol_experiments")).insert;
            _d = [__assign({}, data)];
            _e = {};
            return [4 /*yield*/, supabase.auth.getUser()];
          case 2:
            return [
              4 /*yield*/,
              _c
                .apply(_b, [
                  __assign.apply(
                    void 0,
                    _d.concat([
                      ((_e.created_by =
                        (_f = _g.sent().data.user) === null || _f === void 0 ? void 0 : _f.id),
                      (_e.created_at = new Date().toISOString()),
                      (_e.updated_at = new Date().toISOString()),
                      _e),
                    ]),
                  ),
                ])
                .select()
                .single(),
            ];
          case 3:
            (_a = _g.sent()), (result = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to create protocol experiment: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.updateProtocolExperiment = function (id, data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _b.sent();
            return [
              4 /*yield*/,
              supabase
                .from("protocol_experiments")
                .update(__assign(__assign({}, data), { updated_at: new Date().toISOString() }))
                .eq("id", id)
                .select()
                .single(),
            ];
          case 2:
            (_a = _b.sent()), (result = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to update protocol experiment: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolExperiments = function (filters) {
    return __awaiter(this, void 0, void 0, function () {
      var query, page, per_page, from, to, _a, data, error, count;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            query = supabase.from("protocol_experiments").select("*");
            if (filters === null || filters === void 0 ? void 0 : filters.status) {
              query = query.eq("status", filters.status);
            }
            if (filters === null || filters === void 0 ? void 0 : filters.experiment_type) {
              query = query.eq("experiment_type", filters.experiment_type);
            }
            page = (filters === null || filters === void 0 ? void 0 : filters.page) || 1;
            per_page = (filters === null || filters === void 0 ? void 0 : filters.per_page) || 20;
            from = (page - 1) * per_page;
            to = from + per_page - 1;
            return [4 /*yield*/, query.range(from, to).order("created_at", { ascending: false })];
          case 1:
            (_a = _b.sent()), (data = _a.data), (error = _a.error), (count = _a.count);
            if (error)
              throw new Error("Failed to fetch protocol experiments: ".concat(error.message));
            return [2 /*return*/, { data: data || [], total: count || 0 }];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.analyzeExperimentResults = function (
    experiment_id,
  ) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase,
        _a,
        experiment,
        error,
        _b,
        controlOutcomes,
        testOutcomes,
        controlMean,
        testMean,
        effectSize,
        pooledSampleSize,
        standardError,
        zScore,
        pValue,
        confidenceInterval,
        isSignificant,
        winner;
      return __generator(this, function (_c) {
        switch (_c.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _c.sent();
            return [
              4 /*yield*/,
              supabase.from("protocol_experiments").select("*").eq("id", experiment_id).single(),
            ];
          case 2:
            (_a = _c.sent()), (experiment = _a.data), (error = _a.error);
            if (error) throw new Error("Failed to fetch experiment: ".concat(error.message));
            return [
              4 /*yield*/,
              Promise.all([
                experiment.control_protocol_id
                  ? this.getProtocolOutcomes({
                      protocol_version_id: experiment.control_protocol_id,
                    })
                  : { data: [], total: 0 },
                experiment.test_protocol_id
                  ? this.getProtocolOutcomes({ protocol_version_id: experiment.test_protocol_id })
                  : { data: [], total: 0 },
              ]),
            ];
          case 3:
            (_b = _c.sent()), (controlOutcomes = _b[0]), (testOutcomes = _b[1]);
            controlMean =
              controlOutcomes.data.length > 0
                ? controlOutcomes.data.reduce((sum, o) => sum + o.success_score, 0) /
                  controlOutcomes.data.length
                : 0;
            testMean =
              testOutcomes.data.length > 0
                ? testOutcomes.data.reduce((sum, o) => sum + o.success_score, 0) /
                  testOutcomes.data.length
                : 0;
            effectSize = testMean - controlMean;
            pooledSampleSize = controlOutcomes.data.length + testOutcomes.data.length;
            standardError = Math.sqrt(
              (controlMean * (100 - controlMean)) / controlOutcomes.data.length +
                (testMean * (100 - testMean)) / testOutcomes.data.length,
            );
            zScore = standardError > 0 ? effectSize / standardError : 0;
            pValue = Math.max(0.001, 1 - Math.abs(zScore) / 2);
            confidenceInterval = [
              effectSize - 1.96 * standardError,
              effectSize + 1.96 * standardError,
            ];
            isSignificant = pValue < experiment.significance_level;
            winner = effectSize > 0 ? "test" : "control";
            return [
              2 /*return*/,
              {
                experiment: experiment,
                statistical_analysis: {
                  p_value: pValue,
                  confidence_interval: confidenceInterval,
                  effect_size: effectSize,
                  statistical_power: experiment.statistical_power,
                },
                recommendations: {
                  winner: isSignificant ? winner : undefined,
                  significance: isSignificant,
                  next_steps: isSignificant
                    ? [
                        "Implement ".concat(winner, " protocol"),
                        "Monitor performance",
                        "Plan rollout strategy",
                      ]
                    : [
                        "Continue experiment",
                        "Increase sample size",
                        "Review protocol differences",
                      ],
                },
              },
            ];
        }
      });
    });
  };
  // Evidence and Compliance Management
  AutomatedProtocolOptimizationService.prototype.createProtocolEvidence = function (data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error, _b, _c, _d;
      var _e;
      var _f;
      return __generator(this, (_g) => {
        switch (_g.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _g.sent();
            _c = (_b = supabase.from("protocol_evidence")).insert;
            _d = [__assign({}, data)];
            _e = {};
            return [4 /*yield*/, supabase.auth.getUser()];
          case 2:
            return [
              4 /*yield*/,
              _c
                .apply(_b, [
                  __assign.apply(
                    void 0,
                    _d.concat([
                      ((_e.created_by =
                        (_f = _g.sent().data.user) === null || _f === void 0 ? void 0 : _f.id),
                      (_e.created_at = new Date().toISOString()),
                      (_e.updated_at = new Date().toISOString()),
                      _e),
                    ]),
                  ),
                ])
                .select()
                .single(),
            ];
          case 3:
            (_a = _g.sent()), (result = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to create protocol evidence: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolEvidence = function (protocol_id) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, data, error;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _b.sent();
            return [
              4 /*yield*/,
              supabase
                .from("protocol_evidence")
                .select("*")
                .eq("protocol_id", protocol_id)
                .order("relevance_score", { ascending: false }),
            ];
          case 2:
            (_a = _b.sent()), (data = _a.data), (error = _a.error);
            if (error) throw new Error("Failed to fetch protocol evidence: ".concat(error.message));
            return [2 /*return*/, data || []];
        }
      });
    });
  };
  // Protocol Implementation and Distribution
  AutomatedProtocolOptimizationService.prototype.createProtocolImplementation = function (data) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error, _b, _c, _d;
      var _e;
      var _f;
      return __generator(this, (_g) => {
        switch (_g.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _g.sent();
            _c = (_b = supabase.from("protocol_implementations")).insert;
            _d = [__assign({}, data)];
            _e = {};
            return [4 /*yield*/, supabase.auth.getUser()];
          case 2:
            return [
              4 /*yield*/,
              _c
                .apply(_b, [
                  __assign.apply(
                    void 0,
                    _d.concat([
                      ((_e.implemented_by =
                        (_f = _g.sent().data.user) === null || _f === void 0 ? void 0 : _f.id),
                      (_e.created_at = new Date().toISOString()),
                      (_e.updated_at = new Date().toISOString()),
                      _e),
                    ]),
                  ),
                ])
                .select()
                .single(),
            ];
          case 3:
            (_a = _g.sent()), (result = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to create protocol implementation: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.updateProtocolImplementation = function (
    id,
    data,
  ) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase, _a, result, error;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _b.sent();
            return [
              4 /*yield*/,
              supabase
                .from("protocol_implementations")
                .update(__assign(__assign({}, data), { updated_at: new Date().toISOString() }))
                .eq("id", id)
                .select()
                .single(),
            ];
          case 2:
            (_a = _b.sent()), (result = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to update protocol implementation: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolImplementations = function (
    protocol_version_id,
  ) {
    return __awaiter(this, void 0, void 0, function () {
      var query, _a, data, error;
      return __generator(this, (_b) => {
        switch (_b.label) {
          case 0:
            query = supabase.from("protocol_implementations").select("*");
            if (protocol_version_id) {
              query = query.eq("protocol_version_id", protocol_version_id);
            }
            return [4 /*yield*/, query.order("created_at", { ascending: false })];
          case 1:
            (_a = _b.sent()), (data = _a.data), (error = _a.error);
            if (error)
              throw new Error("Failed to fetch protocol implementations: ".concat(error.message));
            return [2 /*return*/, data || []];
        }
      });
    });
  };
  // Analytics and Reporting
  AutomatedProtocolOptimizationService.prototype.generateProtocolAnalytics = function (request) {
    return __awaiter(this, void 0, void 0, function () {
      var protocol_version_id,
        analytics_period,
        period_start,
        period_end,
        outcomeQuery,
        _a,
        outcomes,
        outcomeError,
        usageCount,
        successRate,
        averageOutcomeScore,
        patientSatisfaction,
        complicationRate,
        analyticsData,
        _b,
        result,
        error;
      return __generator(this, (_c) => {
        switch (_c.label) {
          case 0:
            (protocol_version_id = request.protocol_version_id),
              (analytics_period = request.analytics_period),
              (period_start = request.period_start),
              (period_end = request.period_end);
            outcomeQuery = supabase
              .from("protocol_outcomes")
              .select("*")
              .gte("outcome_date", period_start)
              .lte("outcome_date", period_end);
            if (protocol_version_id) {
              outcomeQuery = outcomeQuery.eq("protocol_version_id", protocol_version_id);
            }
            return [4 /*yield*/, outcomeQuery];
          case 1:
            (_a = _c.sent()), (outcomes = _a.data), (outcomeError = _a.error);
            if (outcomeError)
              throw new Error(
                "Failed to fetch outcomes for analytics: ".concat(outcomeError.message),
              );
            usageCount = (outcomes === null || outcomes === void 0 ? void 0 : outcomes.length) || 0;
            successRate =
              usageCount > 0
                ? outcomes.reduce((sum, o) => sum + o.success_score, 0) / usageCount
                : 0;
            averageOutcomeScore = successRate;
            patientSatisfaction =
              (outcomes === null || outcomes === void 0
                ? void 0
                : outcomes
                    .filter((o) => o.patient_satisfaction)
                    .reduce((sum, o) => sum + (o.patient_satisfaction || 0), 0)) /
              ((outcomes === null || outcomes === void 0
                ? void 0
                : outcomes.filter((o) => o.patient_satisfaction).length) || 1);
            complicationRate =
              usageCount > 0
                ? (outcomes.filter(
                    (o) => Array.isArray(o.complications) && o.complications.length > 0,
                  ).length /
                    usageCount) *
                  100
                : 0;
            analyticsData = {
              protocol_version_id: protocol_version_id || "all_protocols",
              analytics_period: analytics_period,
              period_start: period_start,
              period_end: period_end,
              usage_count: usageCount,
              success_rate: successRate,
              average_outcome_score: averageOutcomeScore,
              patient_satisfaction: patientSatisfaction,
              complication_rate: complicationRate,
              performance_metrics: {
                total_treatments: usageCount,
                successful_treatments:
                  (outcomes === null || outcomes === void 0
                    ? void 0
                    : outcomes.filter((o) => o.success_score >= 75).length) || 0,
                complications:
                  (outcomes === null || outcomes === void 0
                    ? void 0
                    : outcomes.filter(
                        (o) => Array.isArray(o.complications) && o.complications.length > 0,
                      ).length) || 0,
                side_effects:
                  (outcomes === null || outcomes === void 0
                    ? void 0
                    : outcomes.filter(
                        (o) => Array.isArray(o.side_effects) && o.side_effects.length > 0,
                      ).length) || 0,
              },
              calculated_at: new Date().toISOString(),
              created_at: new Date().toISOString(),
            };
            return [
              4 /*yield*/,
              supabase.from("protocol_analytics").insert(analyticsData).select().single(),
            ];
          case 2:
            (_b = _c.sent()), (result = _b.data), (error = _b.error);
            if (error)
              throw new Error("Failed to create protocol analytics: ".concat(error.message));
            return [2 /*return*/, result];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.generatePerformanceReport = function (
    protocol_version_id,
    period_start,
    period_end,
  ) {
    return __awaiter(this, void 0, void 0, function () {
      var _a, protocol, analytics, outcomes, feedback, report;
      return __generator(this, function (_b) {
        switch (_b.label) {
          case 0:
            return [
              4 /*yield*/,
              Promise.all([
                this.getProtocolVersionById(protocol_version_id),
                this.generateProtocolAnalytics({
                  protocol_version_id: protocol_version_id,
                  analytics_period: "monthly",
                  period_start: period_start,
                  period_end: period_end,
                }),
                this.getProtocolOutcomes({ protocol_version_id: protocol_version_id }),
                this.getProtocolFeedback({ protocol_id: protocol_version_id }),
              ]),
            ];
          case 1:
            (_a = _b.sent()),
              (protocol = _a[0]),
              (analytics = _a[1]),
              (outcomes = _a[2]),
              (feedback = _a[3]);
            if (!protocol) {
              throw new Error("Protocol not found for performance report");
            }
            report = {
              protocol_version_id: protocol_version_id,
              reporting_period: {
                start: period_start,
                end: period_end,
              },
              key_metrics: {
                usage_count: analytics.usage_count,
                success_rate: analytics.success_rate || 0,
                patient_satisfaction: analytics.patient_satisfaction || 0,
                provider_satisfaction:
                  feedback.data
                    .filter((f) => f.rating)
                    .reduce((sum, f) => sum + (f.rating || 0), 0) /
                  Math.max(1, feedback.data.filter((f) => f.rating).length),
                cost_effectiveness: analytics.cost_effectiveness || 0,
              },
              trend_analysis: {
                success_trend: "stable", // Simplified - would need historical data for real trends
                usage_trend: "increasing",
                satisfaction_trend: "stable",
              },
              benchmarks: {
                industry_average_success_rate: 75,
                clinic_average_success_rate: 80,
                target_success_rate: 85,
              },
              recommendations: [
                analytics.success_rate && analytics.success_rate < 75
                  ? "Focus on improving success rate through protocol refinement"
                  : "Maintain current performance standards",
                feedback.data.length < 5
                  ? "Encourage more provider feedback for continuous improvement"
                  : "Continue collecting structured feedback",
                analytics.usage_count < 20
                  ? "Consider protocol promotion and training initiatives"
                  : "Monitor implementation consistency",
              ].filter(Boolean),
            };
            return [2 /*return*/, report];
        }
      });
    });
  };
  // Helper Methods
  AutomatedProtocolOptimizationService.prototype.processPriorityFeedback = function (feedback_id) {
    return __awaiter(this, void 0, void 0, function () {
      var supabase;
      return __generator(this, (_a) => {
        switch (_a.label) {
          case 0:
            return [4 /*yield*/, (0, client_1.createClient)()];
          case 1:
            supabase = _a.sent();
            return [
              4 /*yield*/,
              supabase
                .from("protocol_feedback")
                .update({
                  status: "reviewed",
                  reviewed_at: new Date().toISOString(),
                  implementation_notes: "Auto-escalated due to priority level",
                })
                .eq("id", feedback_id),
            ];
          case 2:
            _a.sent();
            return [2 /*return*/];
        }
      });
    });
  };
  AutomatedProtocolOptimizationService.prototype.getProtocolOptimizationAnalysis = function (
    protocol_id,
  ) {
    return __awaiter(this, void 0, void 0, function () {
      var _a,
        protocol,
        outcomes,
        feedback,
        optimizationResults,
        currentPerformance,
        optimizationOpportunities;
      return __generator(this, function (_b) {
        switch (_b.label) {
          case 0:
            return [
              4 /*yield*/,
              Promise.all([
                this.getProtocolVersionById(protocol_id),
                this.getProtocolOutcomes({ protocol_version_id: protocol_id }),
                this.getProtocolFeedback({ protocol_id: protocol_id }),
                supabase.from("optimization_results").select("*").eq("protocol_id", protocol_id),
              ]),
            ];
          case 1:
            (_a = _b.sent()),
              (protocol = _a[0]),
              (outcomes = _a[1]),
              (feedback = _a[2]),
              (optimizationResults = _a[3]);
            if (!protocol) {
              throw new Error("Protocol not found for optimization analysis");
            }
            currentPerformance = {
              success_rate:
                outcomes.data.length > 0
                  ? outcomes.data.reduce((sum, o) => sum + o.success_score, 0) /
                    outcomes.data.length
                  : 0,
              total_outcomes: outcomes.data.length,
              feedback_score:
                feedback.data.filter((f) => f.rating).reduce((sum, f) => sum + (f.rating || 0), 0) /
                Math.max(1, feedback.data.filter((f) => f.rating).length),
              complication_rate:
                outcomes.data.length > 0
                  ? (outcomes.data.filter(
                      (o) => Array.isArray(o.complications) && o.complications.length > 0,
                    ).length /
                      outcomes.data.length) *
                    100
                  : 0,
            };
            optimizationOpportunities = [
              {
                type: "success_rate_optimization",
                potential_improvement: Math.max(0, 85 - currentPerformance.success_rate),
                confidence_score: outcomes.data.length >= 10 ? 85 : 60,
                recommendation:
                  currentPerformance.success_rate < 80
                    ? "Focus on identifying and addressing low-performing protocol steps"
                    : "Fine-tune existing protocol for marginal improvements",
              },
              {
                type: "complication_reduction",
                potential_improvement: Math.max(0, currentPerformance.complication_rate - 5),
                confidence_score: outcomes.data.length >= 20 ? 90 : 65,
                recommendation:
                  currentPerformance.complication_rate > 10
                    ? "Implement additional safety measures and risk mitigation strategies"
                    : "Maintain current safety standards with regular monitoring",
              },
            ].filter((opp) => opp.potential_improvement > 0);
            return [
              2 /*return*/,
              {
                protocol_id: protocol_id,
                current_performance: currentPerformance,
                optimization_opportunities: optimizationOpportunities,
                comparative_analysis: {
                  peer_protocols: "Analysis would include comparison with similar protocols",
                  industry_benchmarks: "Comparison with industry standards",
                  historical_performance: "Trends over time for this protocol",
                },
                risk_assessment: {
                  implementation_risk: "Low to Medium",
                  patient_safety_risk: "Low",
                  operational_risk: "Low",
                  recommendations: [
                    "Implement changes gradually",
                    "Monitor outcomes closely",
                    "Maintain rollback capability",
                  ],
                },
              },
            ];
        }
      });
    });
  };
  return AutomatedProtocolOptimizationService;
})();
exports.AutomatedProtocolOptimizationService = AutomatedProtocolOptimizationService;
