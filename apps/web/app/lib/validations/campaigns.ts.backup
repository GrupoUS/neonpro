// Campaigns Module
export interface CampaignsConfig {
  enabled: boolean;
  data?: any;
}

export const Campaigns_DEFAULT: CampaignsConfig = {
  enabled: true,
  data: null
};

export function createCampaigns() {
  return Campaigns_DEFAULT;
}

export default {
  config: Campaigns_DEFAULT,
  create: createCampaigns
};
