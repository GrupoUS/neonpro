"use client";

import React from 'react';

interface AppointmentreminderProps {
  children?: React.ReactNode;
  className?: string;
}

export function Appointmentreminder({ children, className = "" }: AppointmentreminderProps) {
  return (
    <div className={className}>
      {children || "Appointmentreminder Component"}
    </div>
  );
}

export default Appointmentreminder;
