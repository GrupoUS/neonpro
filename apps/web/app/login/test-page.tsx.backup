"use client";

import React from 'react';

interface TestpageProps {
  children?: React.ReactNode;
  className?: string;
}

export function Testpage({ children, className = "" }: TestpageProps) {
  return (
    <div className={className}>
      {children || "Testpage Component"}
    </div>
  );
}

export default Testpage;
