"use client";

import React from 'react';

interface AlertspanelProps {
  children?: React.ReactNode;
  className?: string;
}

export function Alertspanel({ children, className = "" }: AlertspanelProps) {
  return (
    <div className={className}>
      {children || "Alertspanel Component"}
    </div>
  );
}

export default Alertspanel;
