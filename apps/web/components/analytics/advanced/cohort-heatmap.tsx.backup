"use client";

import React from 'react';

interface CohortheatmapProps {
  children?: React.ReactNode;
  className?: string;
}

export function Cohortheatmap({ children, className = "" }: CohortheatmapProps) {
  return (
    <div className={className}>
      {children || "Cohortheatmap Component"}
    </div>
  );
}

export default Cohortheatmap;
