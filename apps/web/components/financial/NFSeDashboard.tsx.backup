"use client";

import React from 'react';

interface NfsedashboardProps {
  children?: React.ReactNode;
  className?: string;
}

export function Nfsedashboard({ children, className = "" }: NfsedashboardProps) {
  return (
    <div className={className}>
      {children || "Nfsedashboard Component"}
    </div>
  );
}

export default Nfsedashboard;
