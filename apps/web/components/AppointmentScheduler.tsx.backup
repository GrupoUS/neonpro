"use client";

import React from 'react';

interface AppointmentschedulerProps {
  children?: React.ReactNode;
  className?: string;
}

export function Appointmentscheduler({ children, className = "" }: AppointmentschedulerProps) {
  return (
    <div className={className}>
      {children || "Appointmentscheduler Component"}
    </div>
  );
}

export default Appointmentscheduler;
