"use client";

import React from 'react';

interface DaterangepickerProps {
  children?: React.ReactNode;
  className?: string;
}

export function Daterangepicker({ children, className = "" }: DaterangepickerProps) {
  return (
    <div className={className}>
      {children || "Daterangepicker Component"}
    </div>
  );
}

export default Daterangepicker;
