"use client";

import React from 'react';

interface PatientdatacardProps {
  children?: React.ReactNode;
  className?: string;
}

export function Patientdatacard({ children, className = "" }: PatientdatacardProps) {
  return (
    <div className={className}>
      {children || "Patientdatacard Component"}
    </div>
  );
}

export default Patientdatacard;
