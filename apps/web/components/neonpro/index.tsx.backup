"use client";

import React from 'react';

interface IndexProps {
  children?: React.ReactNode;
  className?: string;
}

export function Index({ children, className = "" }: IndexProps) {
  return (
    <div className={className}>
      {children || "Index Component"}
    </div>
  );
}

export default Index;
