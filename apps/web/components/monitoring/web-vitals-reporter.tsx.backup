"use client";

import React from 'react';

interface WebvitalsreporterProps {
  children?: React.ReactNode;
  className?: string;
}

export function Webvitalsreporter({ children, className = "" }: WebvitalsreporterProps) {
  return (
    <div className={className}>
      {children || "Webvitalsreporter Component"}
    </div>
  );
}

export default Webvitalsreporter;
