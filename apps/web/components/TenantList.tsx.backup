"use client";

import React from 'react';

interface TenantlistProps {
  children?: React.ReactNode;
  className?: string;
}

export function Tenantlist({ children, className = "" }: TenantlistProps) {
  return (
    <div className={className}>
      {children || "Tenantlist Component"}
    </div>
  );
}

export default Tenantlist;
