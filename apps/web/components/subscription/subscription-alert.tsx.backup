"use client";

import React from 'react';

interface SubscriptionalertProps {
  children?: React.ReactNode;
  className?: string;
}

export function Subscriptionalert({ children, className = "" }: SubscriptionalertProps) {
  return (
    <div className={className}>
      {children || "Subscriptionalert Component"}
    </div>
  );
}

export default Subscriptionalert;
