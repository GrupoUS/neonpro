"use client";

import React from 'react';

interface BackupdashboardProps {
  children?: React.ReactNode;
  className?: string;
}

export function Backupdashboard({ children, className = "" }: BackupdashboardProps) {
  return (
    <div className={className}>
      {children || "Backupdashboard Component"}
    </div>
  );
}

export default Backupdashboard;
