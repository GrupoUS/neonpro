"use client";

import React from 'react';

interface StoragemonitorProps {
  children?: React.ReactNode;
  className?: string;
}

export function Storagemonitor({ children, className = "" }: StoragemonitorProps) {
  return (
    <div className={className}>
      {children || "Storagemonitor Component"}
    </div>
  );
}

export default Storagemonitor;
