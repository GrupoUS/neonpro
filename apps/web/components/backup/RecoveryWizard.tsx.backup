"use client";

import React from 'react';

interface RecoverywizardProps {
  children?: React.ReactNode;
  className?: string;
}

export function Recoverywizard({ children, className = "" }: RecoverywizardProps) {
  return (
    <div className={className}>
      {children || "Recoverywizard Component"}
    </div>
  );
}

export default Recoverywizard;
