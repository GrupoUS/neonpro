"use client";

import React from 'react';

interface BackuphistoryProps {
  children?: React.ReactNode;
  className?: string;
}

export function Backuphistory({ children, className = "" }: BackuphistoryProps) {
  return (
    <div className={className}>
      {children || "Backuphistory Component"}
    </div>
  );
}

export default Backuphistory;
