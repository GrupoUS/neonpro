"use client";

import React from 'react';

interface ClinicdashboardProps {
  children?: React.ReactNode;
  className?: string;
}

export function Clinicdashboard({ children, className = "" }: ClinicdashboardProps) {
  return (
    <div className={className}>
      {children || "Clinicdashboard Component"}
    </div>
  );
}

export default Clinicdashboard;
