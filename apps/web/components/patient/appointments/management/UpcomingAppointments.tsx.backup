"use client";

import React from 'react';

interface UpcomingappointmentsProps {
  children?: React.ReactNode;
  className?: string;
}

export function Upcomingappointments({ children, className = "" }: UpcomingappointmentsProps) {
  return (
    <div className={className}>
      {children || "Upcomingappointments Component"}
    </div>
  );
}

export default Upcomingappointments;
