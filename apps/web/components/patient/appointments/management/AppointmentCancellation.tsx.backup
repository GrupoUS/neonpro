"use client";

import React from 'react';

interface AppointmentcancellationProps {
  children?: React.ReactNode;
  className?: string;
}

export function Appointmentcancellation({ children, className = "" }: AppointmentcancellationProps) {
  return (
    <div className={className}>
      {children || "Appointmentcancellation Component"}
    </div>
  );
}

export default Appointmentcancellation;
