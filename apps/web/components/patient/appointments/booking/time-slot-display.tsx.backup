"use client";

import React from 'react';

interface TimeslotdisplayProps {
  children?: React.ReactNode;
  className?: string;
}

export function Timeslotdisplay({ children, className = "" }: TimeslotdisplayProps) {
  return (
    <div className={className}>
      {children || "Timeslotdisplay Component"}
    </div>
  );
}

export default Timeslotdisplay;
