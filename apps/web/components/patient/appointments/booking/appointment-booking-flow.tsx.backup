"use client";

import React from 'react';

interface AppointmentbookingflowProps {
  children?: React.ReactNode;
  className?: string;
}

export function Appointmentbookingflow({ children, className = "" }: AppointmentbookingflowProps) {
  return (
    <div className={className}>
      {children || "Appointmentbookingflow Component"}
    </div>
  );
}

export default Appointmentbookingflow;
