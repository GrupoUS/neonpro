"use client";

import React from 'react';

interface ServiceselectionProps {
  children?: React.ReactNode;
  className?: string;
}

export function Serviceselection({ children, className = "" }: ServiceselectionProps) {
  return (
    <div className={className}>
      {children || "Serviceselection Component"}
    </div>
  );
}

export default Serviceselection;
