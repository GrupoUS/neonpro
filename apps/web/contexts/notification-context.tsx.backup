"use client";

import React from 'react';

interface NotificationcontextProps {
  children?: React.ReactNode;
  className?: string;
}

export function Notificationcontext({ children, className = "" }: NotificationcontextProps) {
  return (
    <div className={className}>
      {children || "Notificationcontext Component"}
    </div>
  );
}

export default Notificationcontext;
