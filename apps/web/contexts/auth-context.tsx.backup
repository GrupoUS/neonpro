"use client";

import React from 'react';

interface AuthcontextProps {
  children?: React.ReactNode;
  className?: string;
}

export function Authcontext({ children, className = "" }: AuthcontextProps) {
  return (
    <div className={className}>
      {children || "Authcontext Component"}
    </div>
  );
}

export default Authcontext;
