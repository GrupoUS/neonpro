"use client";

import React from 'react';

interface CrmcontextsupabaseProps {
  children?: React.ReactNode;
  className?: string;
}

export function Crmcontextsupabase({ children, className = "" }: CrmcontextsupabaseProps) {
  return (
    <div className={className}>
      {children || "Crmcontextsupabase Component"}
    </div>
  );
}

export default Crmcontextsupabase;
