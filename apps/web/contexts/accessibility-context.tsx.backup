"use client";

import React from 'react';

interface AccessibilitycontextProps {
  children?: React.ReactNode;
  className?: string;
}

export function Accessibilitycontext({ children, className = "" }: AccessibilitycontextProps) {
  return (
    <div className={className}>
      {children || "Accessibilitycontext Component"}
    </div>
  );
}

export default Accessibilitycontext;
