"use client";

import React from 'react';

interface CrmcontextProps {
  children?: React.ReactNode;
  className?: string;
}

export function Crmcontext({ children, className = "" }: CrmcontextProps) {
  return (
    <div className={className}>
      {children || "Crmcontext Component"}
    </div>
  );
}

export default Crmcontext;
