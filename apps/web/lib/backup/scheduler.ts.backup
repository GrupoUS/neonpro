// Scheduler Module
export interface SchedulerConfig {
  enabled: boolean;
  data?: any;
}

export const Scheduler_DEFAULT: SchedulerConfig = {
  enabled: true,
  data: null
};

export function createScheduler() {
  return Scheduler_DEFAULT;
}

export default {
  config: Scheduler_DEFAULT,
  create: createScheduler
};
