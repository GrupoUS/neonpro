// Backgroundprocessor Module
export interface BackgroundprocessorConfig {
  enabled: boolean;
  data?: any;
}

export const Backgroundprocessor_DEFAULT: BackgroundprocessorConfig = {
  enabled: true,
  data: null
};

export function createBackgroundprocessor() {
  return Backgroundprocessor_DEFAULT;
}

export default {
  config: Backgroundprocessor_DEFAULT,
  create: createBackgroundprocessor
};
