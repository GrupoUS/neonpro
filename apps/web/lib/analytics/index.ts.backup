// Index Module
export interface IndexConfig {
  enabled: boolean;
  data?: any;
}

export const Index_DEFAULT: IndexConfig = {
  enabled: true,
  data: null
};

export function createIndex() {
  return Index_DEFAULT;
}

export default {
  config: Index_DEFAULT,
  create: createIndex
};
