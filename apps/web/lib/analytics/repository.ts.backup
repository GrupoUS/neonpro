// Repository Module
export interface RepositoryConfig {
  enabled: boolean;
  data?: any;
}

export const Repository_DEFAULT: RepositoryConfig = {
  enabled: true,
  data: null
};

export function createRepository() {
  return Repository_DEFAULT;
}

export default {
  config: Repository_DEFAULT,
  create: createRepository
};
