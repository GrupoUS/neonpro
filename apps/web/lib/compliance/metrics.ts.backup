// Metrics Module
export interface MetricsConfig {
  enabled: boolean;
  data?: any;
}

export const Metrics_DEFAULT: MetricsConfig = {
  enabled: true,
  data: null
};

export function createMetrics() {
  return Metrics_DEFAULT;
}

export default {
  config: Metrics_DEFAULT,
  create: createMetrics
};
