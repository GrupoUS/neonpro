// Featureflags Module
export interface FeatureflagsConfig {
  enabled: boolean;
  data?: any;
}

export const Featureflags_DEFAULT: FeatureflagsConfig = {
  enabled: true,
  data: null
};

export function createFeatureflags() {
  return Featureflags_DEFAULT;
}

export default {
  config: Featureflags_DEFAULT,
  create: createFeatureflags
};
