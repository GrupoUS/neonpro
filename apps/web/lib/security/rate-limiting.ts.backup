// Ratelimiting Module
export interface RatelimitingConfig {
  enabled: boolean;
  data?: any;
}

export const Ratelimiting_DEFAULT: RatelimitingConfig = {
  enabled: true,
  data: null
};

export function createRatelimiting() {
  return Ratelimiting_DEFAULT;
}

export default {
  config: Ratelimiting_DEFAULT,
  create: createRatelimiting
};
