// Date Module
export interface DateConfig {
  enabled: boolean;
  data?: any;
}

export const Date_DEFAULT: DateConfig = {
  enabled: true,
  data: null
};

export function createDate() {
  return Date_DEFAULT;
}

export default {
  config: Date_DEFAULT,
  create: createDate
};
