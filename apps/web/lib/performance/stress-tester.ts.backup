// Stresstester Module
export interface StresstesterConfig {
  enabled: boolean;
  data?: any;
}

export const Stresstester_DEFAULT: StresstesterConfig = {
  enabled: true,
  data: null
};

export function createStresstester() {
  return Stresstester_DEFAULT;
}

export default {
  config: Stresstester_DEFAULT,
  create: createStresstester
};
