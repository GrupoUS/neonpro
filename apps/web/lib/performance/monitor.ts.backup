// Monitor Module
export interface MonitorConfig {
  enabled: boolean;
  data?: any;
}

export const Monitor_DEFAULT: MonitorConfig = {
  enabled: true,
  data: null
};

export function createMonitor() {
  return Monitor_DEFAULT;
}

export default {
  config: Monitor_DEFAULT,
  create: createMonitor
};
