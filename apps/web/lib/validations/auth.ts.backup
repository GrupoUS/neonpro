// Auth Module
export interface AuthConfig {
  enabled: boolean;
  data?: any;
}

export const Auth_DEFAULT: AuthConfig = {
  enabled: true,
  data: null
};

export function createAuth() {
  return Auth_DEFAULT;
}

export default {
  config: Auth_DEFAULT,
  create: createAuth
};
