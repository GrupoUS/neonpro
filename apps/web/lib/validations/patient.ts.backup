// Patient Module
export interface PatientConfig {
  enabled: boolean;
  data?: any;
}

export const Patient_DEFAULT: PatientConfig = {
  enabled: true,
  data: null
};

export function createPatient() {
  return Patient_DEFAULT;
}

export default {
  config: Patient_DEFAULT,
  create: createPatient
};
