// Sessionconfig Module
export interface SessionconfigConfig {
  enabled: boolean;
  data?: any;
}

export const Sessionconfig_DEFAULT: SessionconfigConfig = {
  enabled: true,
  data: null
};

export function createSessionconfig() {
  return Sessionconfig_DEFAULT;
}

export default {
  config: Sessionconfig_DEFAULT,
  create: createSessionconfig
};
