// Session Module
export interface SessionConfig {
  enabled: boolean;
  data?: any;
}

export const Session_DEFAULT: SessionConfig = {
  enabled: true,
  data: null
};

export function createSession() {
  return Session_DEFAULT;
}

export default {
  config: Session_DEFAULT,
  create: createSession
};
