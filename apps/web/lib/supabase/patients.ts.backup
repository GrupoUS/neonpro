// Patients Module
export interface PatientsConfig {
  enabled: boolean;
  data?: any;
}

export const Patients_DEFAULT: PatientsConfig = {
  enabled: true,
  data: null
};

export function createPatients() {
  return Patients_DEFAULT;
}

export default {
  config: Patients_DEFAULT,
  create: createPatients
};
