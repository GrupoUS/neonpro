"use client";

import React from 'react';

interface BillingProps {
  children?: React.ReactNode;
  className?: string;
}

export function Billing({ children, className = "" }: BillingProps) {
  return (
    <div className={className}>
      {children || "Billing Component"}
    </div>
  );
}

export default Billing;
