"use client";

import React from 'react';

interface IntegrationProps {
  children?: React.ReactNode;
  className?: string;
}

export function Integration({ children, className = "" }: IntegrationProps) {
  return (
    <div className={className}>
      {children || "Integration Component"}
    </div>
  );
}

export default Integration;
